import 'package:flutter/cupertino.dart';

class BillOerder extends StatelessWidget {
  const BillOerder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "账单"
      ),
    );
  }
}