import 'package:flutter/cupertino.dart';

class ItemBillOerder extends StatelessWidget {
  const ItemBillOerder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "账单"
      ),
    );
  }
}