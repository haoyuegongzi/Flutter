import 'package:flutter/cupertino.dart';

class ItemMinePage extends StatelessWidget {
  const ItemMinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "我的界面"
      ),
    );
  }
}

