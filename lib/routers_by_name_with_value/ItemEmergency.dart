import 'package:flutter/cupertino.dart';

class ItemEmergency extends StatelessWidget {
  const ItemEmergency({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "紧急事件"
      ),
    );
  }
}