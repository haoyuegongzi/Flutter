import 'package:flutter/cupertino.dart';

class RootRoutrsBillOrder extends StatelessWidget {
  const RootRoutrsBillOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "账单"
      ),
    );
  }
}