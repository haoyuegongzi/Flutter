import 'package:flutter/cupertino.dart';

class RootRoutrsEmergency extends StatelessWidget {
  const RootRoutrsEmergency({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "紧急事件"
      ),
    );
  }
}