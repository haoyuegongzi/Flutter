import 'package:flutter/cupertino.dart';

class RootRoutrsMine extends StatelessWidget {
  const RootRoutrsMine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "我的界面"
      ),
    );
  }
}

