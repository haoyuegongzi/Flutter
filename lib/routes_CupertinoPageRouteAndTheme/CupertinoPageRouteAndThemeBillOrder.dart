import 'package:flutter/cupertino.dart';

class CupertinoPageRouteAndThemeBillOrder extends StatelessWidget {
  const CupertinoPageRouteAndThemeBillOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "账单"
      ),
    );
  }
}