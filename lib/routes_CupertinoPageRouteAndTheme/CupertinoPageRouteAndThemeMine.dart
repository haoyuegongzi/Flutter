import 'package:flutter/cupertino.dart';

class CupertinoPageRouteAndThemeMine extends StatelessWidget {
  const CupertinoPageRouteAndThemeMine({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
          "我的界面"
      ),
    );
  }
}

